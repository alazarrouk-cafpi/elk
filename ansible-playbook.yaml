---
- hosts: localhost
  connection: local
  tasks:
    - name: Creating resource group
      azure_rm_webapp:
        resource_group: "RG-PFE"
        name: "mocazi-app"
        app_state : stopped
      register: webapp
    - name: Display result
      debug:
        var: webapp