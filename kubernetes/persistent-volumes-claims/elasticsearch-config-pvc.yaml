apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esconfig-pvc
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany 
  resources:
    requests:
      storage: 50Mi
  volumeName: esconfig-pv