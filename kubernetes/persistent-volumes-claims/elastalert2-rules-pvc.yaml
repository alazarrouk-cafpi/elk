apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elastalert2-rules-pvc
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany 
  resources:
    requests:
      storage: 50Mi
  volumeName: elastalert2-rules-pv